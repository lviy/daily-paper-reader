---
title: "DualDiffusion: A Speculative Decoding Strategy for Masked Diffusion Models"
title_zh: DualDiffusion：一种针对掩码扩散模型的投机解码策略
authors: "Satyam Goyal, Kushal Patel, Tanush Mittal, Arjun Laxman"
date: 2026-04-06
pdf: "https://arxiv.org/pdf/2604.05250v1"
tags: ["query:sd-llm"]
score: 9.0
evidence: 针对掩码扩散模型的投机解码框架，结合了草稿模型与验证模型
tldr: 掩码扩散模型（MDM）虽支持并行生成，但因双向注意力机制无法利用KV缓存，导致推理计算开销巨大。现有加速方案常以牺牲质量为代价。本文提出DualDiffusion，一种针对MDM的投机解码框架，通过轻量级草稿模型快速生成候选序列，再由精确验证模型进行校验。实验表明，该方法在MMLU和GSM8K任务上显著提升了推理效率，同时保持了极高的生成质量，实现了更优的性能平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05250v1/fig-001.webp\", \"caption\": \"Figure 1. Accuracy vs. runtime on MMLU. DualDiffusion (orange) achieves near-verifier accuracy at significantly reduced latency.\", \"page\": 5, \"index\": 1, \"width\": 492, \"height\": 306}, {\"url\": \"assets/figures/arxiv/2604.05250v1/fig-002.webp\", \"caption\": \"Figure 2. Accuracy comparison on GSM8K. DualDiffusion underperforms on mathematical reasoning, indicating sensitivity to drafter quality in multi-step tasks.\", \"page\": 6, \"index\": 2, \"width\": 492, \"height\": 369}]"
motivation: 掩码扩散模型因双向注意力机制导致推理时计算复杂度高，且现有加速技术往往会损害生成质量。
method: 提出一种投机解码框架，结合高效的轻量级草稿模型进行多步预测与精确的验证模型进行单步校验。
result: 在MMLU和GSM8K基准测试中，该方法在减少生成步数的同时维持了高准确率，优于现有的加速策略。
conclusion: DualDiffusion成功优化了掩码扩散模型在质量与效率之间的权衡，为高效扩散语言模型推理提供了新方案。
---

## 摘要
掩码扩散模型（MDMs）通过实现并行 Token 生成和双向上下文建模，为自回归语言模型提供了一种极具前景的替代方案。然而，由于双向注意力机制无法缓存键值对（KV Cache），其推理速度受到显著限制，导致每个生成步骤都需要 $O(N^2)$ 的计算量。虽然 FastDLLM 和 DkvCache 等近期方法通过注意力近似和缓存策略提高了推理速度，但它们是以牺牲生成质量为代价来实现加速的。我们提出了 DualDiffusion，这是一种针对 MDMs 的投机解码框架，它将快速的草稿模型（使用高效近似）与较慢但更准确的验证模型相结合。通过运行轻量级草稿模型的多个步骤，随后进行单次验证步骤，DualDiffusion 与现有方法相比，在生成步数和准确性之间实现了更优的帕累托前沿（Pareto frontier）。我们在 MMLU 和 GSM8K 上评估了我们的方法，结果表明 DualDiffusion 在保持高准确性的同时减少了所需的生成步数，有效地优化了掩码扩散语言模型的质量-效率权衡曲线。

## Abstract
Masked Diffusion Models (MDMs) offer a promising alternative to autoregressive language models by enabling parallel token generation and bidirectional context modeling. However, their inference speed is significantly limited by the inability to cache key-value pairs due to bidirectional attention, requiring $O(N^2)$ computations at each generation step. While recent methods like FastDLLM and DkvCache improve inference speed through attention approximations and caching strategies, they achieve speedups at the cost of generation quality. We propose DualDiffusion, a speculative decoding framework for MDMs that combines fast drafter models (using efficient approximations) with slower, more accurate verifier models. By running multiple steps of a lightweight drafter followed by a single verification step, DualDiffusion achieves a superior Pareto frontier between generation steps and accuracy compared to existing approaches. We evaluate our method on MMLU and GSM8K, demonstrating that DualDiffusion maintains high accuracy while reducing the number of generation steps required, effectively pushing the quality-efficiency trade-off curve for masked diffusion language models.

---

## 论文详细总结（自动生成）

这是一份关于论文《DualDiffusion: A Speculative Decoding Strategy for Masked Diffusion Models》的深度结构化总结：

### 1. 核心问题与研究背景
*   **研究动机**：掩码扩散模型（MDMs）相比自回归（AR）模型具有并行生成和双向上下文建模的优势。然而，MDM 的双向注意力机制导致其无法像 AR 模型那样使用键值（KV）缓存，每一生成步的计算复杂度高达 $O(N^2)$。
*   **核心问题**：现有的加速方案（如 FastDLLM、dKV-Cache）通过近似注意力或固定 Token 表示来换取速度，但这会破坏 MDM 的迭代优化能力，导致生成质量显著下降。
*   **整体含义**：本文旨在解决 MDM 推理中“速度与质量”的矛盾，探索如何将投机解码（Speculative Decoding）引入非自回归的扩散模型框架中。

### 2. 方法论：DualDiffusion 框架
*   **核心思想**：结合一个快速但精度较低的“草稿模型”（Drafter）和一个慢速但高精度的“验证模型”（Verifier）。草稿模型连续运行 $K$ 步生成候选 Token，验证模型仅在第 $K$ 步进行一次前向计算，对候选结果进行校验和修正。
*   **关键技术细节**：
    *   **草稿阶段**：使用集成加速技术（如 FastDLLM 的块状缓存）的轻量级模型快速推进扩散步。
    *   **验证阶段**：验证模型计算全序列的概率分布，并根据验证算法决定是否保留草稿结果。
*   **三种验证算法**：
    1.  **Trust Verifier**：直接接受草稿输出（基准对比项）。
    2.  **KL 散度重掩码（KL Divergence-based）**：计算草稿与验证模型分布间的 KL 散度，超过阈值或按概率对差异大的 Token 进行重新掩码（Remask）。
    3.  **置信度重掩码（Confidence-based）**：仅依据验证模型自身的置信度，对低置信度位置进行重掩码。

### 3. 实验设计
*   **数据集/场景**：
    *   **MMLU**：评估多任务逻辑推理能力。
    *   **GSM8K**：评估多步数学算术推理能力。
*   **Benchmark 与对比方法**：
    *   **LLaDA**：全量验证模型（高精度、低速度基准）。
    *   **FastDLLM**：纯草稿模型加速方案（低精度、高速度基准）。
    *   **DualDiffusion**：本文提出的投机解码方案。

### 4. 资源与算力
*   **硬件环境**：实验在 **NVIDIA A40 GPU** 上运行。
*   **配置细节**：推理时 Batch Size 为 1。
*   **算力说明**：文中未明确提及模型训练时长（主要关注推理加速），但指出 DualDiffusion 的峰值显存占用约为 **31.7 GB**，几乎是单一模型（约 16 GB）的两倍，因为它需要同时加载草稿和验证两个模型。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准数据集上进行了端到端性能测试，并针对验证算法、加速比和准确率进行了对比分析。
*   **充分性评价**：实验设计较为客观，直接对比了当前最先进的 MDM 加速方案。但在消融实验和超参数（如 $K$ 值、不同草稿模型组合）的覆盖上相对简略，且目前仅验证了逻辑和数学两个领域。

### 6. 主要结论与发现
*   **帕累托前沿优化**：在 MMLU 上，DualDiffusion 达到了 0.47 的准确率（接近 LLaDA 的 0.48），同时实现了 **3.9倍** 的加速。
*   **任务敏感性**：在 GSM8K 数学任务上表现不佳（准确率仅 0.25，远低于验证器的 0.57），说明单步验证难以纠正数学推理中的级联错误。
*   **效率提升**：证明了投机解码可以有效摊销高精度 MDM 的计算成本。

### 7. 优点与亮点
*   **首创性**：首次将投机解码引入掩码扩散语言模型领域，打破了该领域长期存在的质量-速度权衡僵局。
*   **灵活性**：验证算法（如 KL 散度法）能够动态识别并修正草稿模型的错误，保留了 MDM 的迭代细化特性。
*   **即插即用**：该框架可以兼容现有的各种 MDM 加速技术作为草稿模型。

### 8. 不足与局限
*   **数学推理能力弱**：在需要极高精确度的多步推理任务（如 GSM8K）中，单次验证的纠错能力有限，错误容易在双向上下文中扩散。
*   **显存开销大**：需要同时驻留两个模型，显存占用翻倍，限制了在消费级硬件上的部署。
*   **验证步数固定**：目前采用固定的 $K$ 步验证，缺乏根据生成内容难度动态调整验证频率的机制。

（完）
