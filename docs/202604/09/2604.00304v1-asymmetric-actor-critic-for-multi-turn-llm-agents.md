---
title: Asymmetric Actor-Critic for Multi-turn LLM Agents
title_zh: 面向多轮 LLM 智能体的非对称 Actor-Critic 框架
authors: "Shuli Jiang, Zhaoyang Zhang, Yi Zhang, Shuo Yang, Wei Xia, Stefano Soatto"
date: 2026-03-31
pdf: "https://arxiv.org/pdf/2604.00304v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于多轮LLM智能体的不对称Actor-Critic框架
tldr: 针对多轮对话智能体在单次尝试场景下可靠性不足的问题，本文提出一种非对称 Actor-Critic 框架。该框架利用大型闭源模型作为 Actor，配合小型开源模型作为 Critic 进行实时监督与干预。通过生成-验证不对称性，利用微调后的小型 Critic 提升任务成功率，在 τ-bench 等基准测试中表现优异，为提升闭源模型智能体的可靠性提供了高效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.00304v1/fig-001.webp\", \"caption\": \"Figure 1: Existing actor–critic methods assume trainable actors and structured action spaces. We propose an asymmetric actor–critic framework where a lightweight open-source critic supervises a fixed proprietary actor at runtime, enabling reliable control for one-shot multiturn conversational agents.\", \"page\": 1, \"index\": 1, \"width\": 458, \"height\": 291}, {\"url\": \"assets/figures/arxiv/2604.00304v1/fig-002.webp\", \"caption\": \"Table 13: Statistics of the generated critic training data. Each critic evaluation point in a trajectory corresponds to one training sample. Positive samples denote critic rejections, while negative samples denote approvals. For UserBench, the critic is trained on data aggregated across all environments.\", \"page\": 18, \"index\": 2, \"width\": 779, \"height\": 204}]"
motivation: 现有的多轮对话智能体在无法重试的单次交互场景中可靠性较差，且难以在不修改闭源模型的前提下实现有效监督。
method: 提出非对称 Actor-Critic 框架，由大型闭源模型负责生成，小型开源模型作为 Critic 在同一轨迹内进行实时监控和干预，并配套了 Critic 微调的数据生成流水线。
result: 在 τ-bench 和 UserBench 上的实验表明，该方法显著提升了任务成功率，且微调后的小型开源 Critic 性能可媲美甚至超越大型闭源模型。
conclusion: 该研究证明了利用小型模型对大型闭源模型进行实时监督的可行性，通过非对称架构有效增强了多轮对话智能体的可靠性。
---

## 摘要
大语言模型（LLM）展现出强大的推理和对话能力，但在多轮交互中确保其行为的可靠性仍具挑战。在许多实际应用中，智能体必须在无法重试的单次尝试场景下取得成功。现有方法要么依赖于反思或事后评估（这需要额外的尝试），要么假设模型完全可训练（从而无法利用闭源专有 LLM）。我们提出了一种用于构建可靠对话智能体的非对称 Actor-Critic 框架。其中，强大的闭源 LLM 担任 Actor，而较小的开源 Critic 提供运行时监督，监控 Actor 的动作并在同一交互轨迹内进行干预。与基于训练的 Actor-Critic 方法不同，我们的框架监督的是在开放式对话环境中运行的固定 Actor。该设计利用了生成与验证的不对称性：虽然高质量生成需要大模型，但有效的监督往往可以通过较小的模型实现。我们还引入了一个数据生成流水线，在不修改 Actor 的情况下产生用于 Critic 微调的监督信号。在 $τ$-bench 和 UserBench 上的实验表明，我们的方法相比强大的单智能体基线显著提高了可靠性和任务成功率。此外，轻量级开源 Critic 在担任 Critic 角色时可以媲美甚至超越更大的闭源模型，且 Critic 微调在多种最先进方法的基础上带来了额外增益。

## Abstract
Large language models (LLMs) exhibit strong reasoning and conversational abilities, but ensuring reliable behavior in multi-turn interactions remains challenging. In many real-world applications, agents must succeed in one-shot settings where retries are impossible. Existing approaches either rely on reflection or post-hoc evaluation, which require additional attempts, or assume fully trainable models that cannot leverage proprietary LLMs. We propose an asymmetric actor-critic framework for reliable conversational agents. A powerful proprietary LLM acts as the actor, while a smaller open-source critic provides runtime supervision, monitoring the actor's actions and intervening within the same interaction trajectory. Unlike training-based actor-critic methods, our framework supervises a fixed actor operating in open-ended conversational environments. The design leverages a generation-verification asymmetry: while high-quality generation requires large models, effective oversight can often be achieved by smaller ones. We further introduce a data generation pipeline that produces supervision signals for critic fine-tuning without modifying the actor. Experiments on $τ$-bench and UserBench show that our approach significantly improves reliability and task success over strong single-agent baselines. Moreover, lightweight open-source critics rival or surpass larger proprietary models in the critic role, and critic fine-tuning yields additional gains over several state-of-the-art methods.

---

## 论文详细总结（自动生成）

这篇论文介绍了一种名为 **Asymmetric Actor-Critic (Asym-AC)** 的框架，旨在提升多轮对话 LLM 智能体在实际应用中的可靠性。以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心挑战**：尽管 LLM 智能体能力强大，但在多轮交互中常出现幻觉、违反业务规则或逻辑不一致等问题。在许多现实场景（如客户服务）中，系统通常只有一次机会（One-shot），无法通过多次重试或事后反思来纠正错误。
*   **现有局限**：
    *   **反思类方法**：依赖多次尝试或后处理，不适合实时交互。
    *   **传统 Actor-Critic 训练**：假设 Actor 是可训练的开源模型，且动作空间是结构化的（如点击、选择），难以应用于闭源 API 模型（如 GPT-4, Claude-3.5）和开放式语言对话。
*   **研究动机**：利用“生成与验证的不对称性”（生成高质量内容很难，但验证其正确性相对容易），通过一个小型的、可微调的开源模型来监督一个强大的、固定的闭源模型。

### 2. 方法论：核心思想与技术细节
*   **非对称架构**：
    *   **Actor ($\pi_A$)**：强大的闭源 LLM（如 Claude-3.5 Sonnet），负责生成对话、调用工具和决策。其参数固定。
    *   **Critic ($C_\theta$)**：轻量级开源模型（如 Qwen3-8B），负责运行时监督。
*   **运行时干预流程**：
    1.  Actor 提出一个候选动作（如回复用户或调用 API）。
    2.  Critic 评估该动作。如果发现潜在错误（违反政策、偏好不匹配等），则生成反馈。
    3.  Actor 根据 Critic 的反馈在同一轮次内修改动作，最后执行修改后的动作。
*   **Critic 导向的数据生成流水线**：
    *   **任务重放**：从现有对话数据中推断任务规范，让 Actor 重新执行。
    *   **硬任务过滤**：筛选出单 Actor 容易失败但在 Critic 帮助下能成功的案例。
    *   **自博弈收集**：记录 Critic 的干预决策和反馈，构建监督微调（SFT）数据集，使 Critic 学习何时以及如何纠正 Actor。

### 3. 实验设计
*   **数据集/场景**：
    *   **$\tau$-bench**：模拟零售和航空领域的客户服务，强调政策合规性和工具调用。
    *   **UserBench**：模拟旅游规划，强调从对话中推断用户隐式偏好并提供最优推荐。
*   **对比方法**：
    *   **Single Actor**：仅使用 Claude-3.5 Sonnet。
    *   **CRITIC**：基于提示词的自我修正方法。
    *   **ReAct**：经典的推理-动作交替框架。
    *   **APIGen-MT / IRMA**：针对 $\tau$-bench 的特定基准方法。
    *   **不同规模的 Critic**：对比了 Claude-4, Qwen3-4B/8B/32B 作为 Critic 的表现。

### 4. 资源与算力
*   **模型使用**：Actor 统一使用 Claude-4 Sonnet（注：文中提及的 Claude-4 可能是指当时的最强版本或特定代号）。Critic 使用了 Qwen3 系列。
*   **训练细节**：使用了 TRL 框架进行 LoRA 微调。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或总训练时长。但提到了微调参数（Rank=16, Alpha=32, Epochs=5-15），属于典型的参数高效微调规模。

### 5. 实验数量与充分性
*   **实验规模**：在两个主流基准测试的多个子领域（零售、航空、T-22/33/44）进行了评估。
*   **充分性**：
    *   涵盖了**零样本监督**和**微调后监督**两种情况。
    *   进行了**消融实验**，验证了 Critic 模型规模对结果的影响。
    *   每项任务均进行了 5 次独立运行并取平均值，保证了结果的统计客观性。
    *   实验设计较为公平，所有对比方法均基于相同的强 Actor 模型。

### 6. 主要结论与发现
*   **非对称有效性**：引入轻量级 Critic 显著提升了任务成功率（在 $\tau$-bench 上平均提升约 5%）。
*   **规模不对称性**：8B 或 32B 的开源 Critic 在监督任务上的表现**优于**同样作为 Critic 的巨型闭源模型（Claude-4），证明了专用小模型在验证任务上的潜力。
*   **微调增益**：通过论文提出的流水线微调后的 Critic（Asym-AC-SFT）表现最佳，证明了任务特定定制化的必要性。

### 7. 优点
*   **实用性强**：无需访问闭源模型的权重即可实现行为控制，非常适合企业级应用。
*   **低延迟/低成本**：相比于让大模型自我反思，使用小模型作为 Critic 降低了推理成本和响应延迟。
*   **单次成功率**：专注于“一次性成功”，解决了多轮对话中错误累积且无法回溯的痛点。

### 8. 不足与局限
*   **Actor 依赖性**：如果 Actor 本身的推理能力极差，即使 Critic 指出错误，Actor 可能也无法理解反馈并做出正确修正。
*   **计算开销**：虽然 Critic 很小，但每轮交互仍需额外的推理步骤，增加了系统的整体延迟。
*   **干预时机**：目前的干预逻辑（$\delta_t$）在某些场景下依赖于启发式规则（如检测到 API 调用时才干预），可能无法覆盖所有潜在的对话错误。
*   **领域局限**：实验主要集中在客服和规划类任务，对于更开放、无明确规则约束的创意类对话效果尚待验证。

（完）
