---
title: "ProCeedRL: Process Critic with Exploratory Demonstration Reinforcement Learning for LLM Agentic Reasoning"
title_zh: ProCeedRL：结合探索性演示强化学习的过程批判器，用于大语言模型智能体推理
authors: "Jingyue Gao, Yanjiang Guo, Xiaoshuai Chen, Jianyu Chen"
date: 2026-04-02
pdf: "https://arxiv.org/pdf/2604.02006v1"
tags: ["query:ag-rl"]
score: 10.0
evidence: 用于多轮大语言模型智能体推理任务的强化学习
tldr: 针对多轮智能体任务中因错误累积导致的探索失效问题，本文提出ProCeedRL框架。该方法通过引入过程级评论家实时监控交互，并结合基于反思的探索性演示进行主动干预，有效打破了错误反馈循环。实验证明，ProCeedRL在深度搜索和具身任务中显著提升了探索效率和模型性能，解决了长程推理中的随机性挑战。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.02006v1/fig-001.webp\", \"caption\": \"Table 2: Main results on the embodied AlfWorld task.\", \"page\": 6, \"index\": 1, \"width\": 458, \"height\": 295}, {\"url\": \"assets/figures/arxiv/2604.02006v1/fig-002.webp\", \"caption\": \"Figure 4: Pass@k of ProCeed and vanilla rollout. The x-axis denotes the number of equivalent vanilla samples in terms of generation. Our method significantly improves exploration efficiency, matching pass@k with less computation. Notably, it exceeds the saturation ceiling of vanilla exploration with a few samples (denoted by stars).\", \"page\": 7, \"index\": 2, \"width\": 526, \"height\": 275}, {\"url\": \"assets/figures/arxiv/2604.02006v1/fig-003.webp\", \"caption\": \"Figure 2: Comparison between standard independently repeated sampling (a) and ProCeedRL rollouts (b). Left: In vanilla exploration, the model’s suboptimal action may result in irrelevant or misleading observations, which hinder all subsequent reasoning and the exploration of correct samples. Right: In ProCeedRL, a critic actively monitors the planning process. When an adverse action is detected due to faulty reasoning or low-quality returned observations, a refined demonstration replaces it to guide the agent out of the vicious circle in (a) and mitigate the exploration issue.\", \"page\": 3, \"index\": 3, \"width\": 921, \"height\": 184}, {\"url\": \"assets/figures/arxiv/2604.02006v1/fig-004.webp\", \"caption\": \"Figure 6: Ablation on rewind thresholds (lth). Blue histograms display the critic score distributions, and the orange line shows rollout accuracy for rewinding and refining all steps ≤ lth. While pruning low-scoring steps improves exploration, excessive rewinding at higher thresholds yields diminishing returns and performance degradation.\", \"page\": 8, \"index\": 4, \"width\": 868, \"height\": 233}, {\"url\": \"assets/figures/arxiv/2604.02006v1/fig-005.webp\", \"caption\": \"Figure 3: The overall workflow of our method with an example. (a) When the agent makes a suboptimal action, the observation reinforces this path and derails subsequent model reasoning, resulting in low exploration efficiency (Sec. 3.2). We employ a process-level critic to identify flawed steps, in which the agent refines its actions and reruns the adverse actions, thereby breaking the vicious circle and improving exploration and reasoning limits (Sec. 3.3). (b) After collecting trajectories with ProCeed rollout, we incorporate them with directly generated samples to form meaningful groups for subsequent policy optimization in (c), as detailedly described in Sec. 3.4.\", \"page\": 4, \"index\": 5, \"width\": 940, \"height\": 544}]"
motivation: 多轮智能体任务中，次优动作引发的错误累积和环境随机性会导致探索陷入僵局，使标准强化学习策略失效。
method: 提出ProCeedRL，利用过程级评论家实时监控交互，并引入基于反思的演示来主动引导智能体纠正错误。
result: 该方法显著超越了模型的饱和探索性能，在复杂深度搜索和具身任务中取得了优异的表现。
conclusion: 通过将探索从被动选择转变为主动干预，ProCeedRL有效解决了长程智能体推理中的错误累积问题，提升了学习效率。
---

## 摘要
强化学习（RL）显著增强了大语言模型（LLMs）的推理能力，但由于交互的长程特性和环境反馈的随机性，将其应用于多轮智能体任务仍然具有挑战性。我们识别出智能体探索中的一种结构性失效模式：次优动作会引发带有噪声的观测并进入误导性的上下文，从而进一步削弱后续的决策，使恢复变得越来越困难。这种错误的累积反馈循环使得标准的探索策略失效，且容易受到模型推理和环境随机性的影响。为了缓解这一问题，我们提出了 ProCeedRL：结合探索性演示强化学习的过程批判器，将探索从被动选择转变为主动干预。ProCeedRL 采用过程级批判器实时监控交互，并结合基于反思的演示来引导智能体停止错误的累积。我们发现，该方法显著超过了模型的饱和探索性能，展示了实质性的探索收益。通过从探索性演示和同策略（on-policy）样本中学习，ProCeedRL 显著提高了探索效率，并在复杂的深度搜索和具身任务中取得了卓越的性能。

## Abstract
Reinforcement Learning (RL) significantly enhances the reasoning abilities of large language models (LLMs), yet applying it to multi-turn agentic tasks remains challenging due to the long-horizon nature of interactions and the stochasticity of environmental feedback. We identify a structural failure mode in agentic exploration: suboptimal actions elicit noisy observations into misleading contexts, which further weaken subsequent decision-making, making recovery increasingly difficult. This cumulative feedback loop of errors renders standard exploration strategies ineffective and susceptible to the model's reasoning and the environment's randomness. To mitigate this issue, we propose ProCeedRL: Process Critic with Explorative Demonstration RL, shifting exploration from passive selection to active intervention. ProCeedRL employs a process-level critic to monitor interactions in real time, incorporating reflection-based demonstrations to guide agents in stopping the accumulation of errors. We find that this approach significantly exceeds the model's saturated exploration performance, demonstrating substantial exploratory benefits. By learning from exploratory demonstrations and on-policy samples, ProCeedRL significantly improves exploration efficiency and achieves superior performance on complex deep search and embodied tasks.

---

## 论文详细总结（自动生成）

这是一份关于论文《ProCeedRL: Process Critic with Exploratory Demonstration Reinforcement Learning for LLM Agentic Reasoning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文聚焦于大语言模型（LLM）在**多轮智能体推理任务**（如深度搜索、具身交互）中的失效模式。作者识别出一个“**恶性循环**”：
*   **错误累积效应**：在长程交互中，智能体的一个次优动作会引发环境返回带有噪声或误导性的观测，这些信息被加入上下文后，会进一步削弱模型后续的推理能力，导致模型陷入无法恢复的错误路径。
*   **标准 RL 的局限**：传统的强化学习（如 RLVR）依赖独立重复采样进行探索，但在多轮任务中，这种被动采样很难突破模型自身的推理上限，探索效率极低。
*   **研究目标**：提出一种从“被动选择”转向“主动干预”的强化学习框架，通过实时监控和纠偏来打破错误循环。

### 2. 方法论：ProCeedRL 框架
核心思想是在训练的 Rollout（采样）阶段引入**过程级批判器（Process Critic）**和**探索性演示（Exploratory Demonstration）**。

*   **过程级批判器（Process-Level Critic）**：在每个步骤 $t$，批判器 $\phi$ 对当前动作 $a_t$ 和环境反馈 $s_{t+1}$ 进行评估，输出评分 $l_t$ 和文本分析 $c_t$。
*   **回溯与修正（Rewind & Refine）**：如果评分低于预设阈值 $l_{th}$，系统会触发干预：
    1.  **回溯**：撤销该错误步骤。
    2.  **修正**：利用修正策略 $\mu$（可以是模型自身或更强的模型）结合批判器的反馈生成一个“精炼动作” $a'_t$。
    3.  **演示**：将此精炼动作作为演示引导智能体继续后续步骤。
*   **策略优化**：
    *   将收集到的“修正轨迹”与“原始同策略轨迹”共同存入 Buffer。
    *   使用 **DAPO** 或 **SFT** 进行优化。
    *   **分布偏移处理**：引入 `chord-φ` 方法，通过动态权重 $\sigma$ 调整演示样本的损失，以平衡同策略（On-policy）数据与离策略（Off-policy）演示数据之间的分布差异。

### 3. 实验设计
*   **场景与数据集**：
    *   **深度搜索 QA**：使用 MuSiQue、WebWalkerQA、GAIA、Frames 和 Bamboogle 等基准测试，要求模型通过多轮搜索工具调用回答复杂问题。
    *   **具身智能体**：使用 ALFWorld 任务，要求模型在虚拟家居环境中进行长程规划和操作。
*   **Benchmark 与对比方法**：
    *   **基座模型**：Qwen3-1.7B/8B。
    *   **对比算法**：SFT-only、RFT（拒绝采样微调）、标准 RL 算法（GRPO、DAPO）以及相关的智能体强化学习工作（如 GiGPO、DeepResearcher）。

### 4. 资源与算力
*   **硬件**：使用 Nvidia H100 和 A100-80G GPU。
*   **算力消耗**：总计算预算约为 **10 个 GPU 月**。
*   **配置细节**：1.7B 模型训练通常使用 2 张显卡，8B 模型使用 8 张显卡。
*   **开销分析**：由于引入了批判器和修正步骤，ProCeedRL 的采样成本约为普通采样的 **1.8 到 2.5 倍**（以生成的 Token 数量计）。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个 QA 数据集和 1 个具身任务上进行了测试，每项实验均报告了三次运行的平均准确率。
*   **消融实验**：
    *   **批判器类型**：对比了自批判（Self-critic）、同族大模型批判和外部强模型批判的效果。
    *   **回溯阈值**：分析了不同评分阈值对最终性能的影响。
    *   **探索效率**：通过 Pass@k 曲线对比了 ProCeedRL 与传统采样在相同计算量下的表现。
*   **评价**：实验设计较为全面，涵盖了从短程搜索到长程规划的不同维度，且通过等效计算量对比证明了其效率提升，具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **突破上限**：ProCeedRL 能够突破模型在标准 RL 探索下的性能饱和点（Ceiling），在相同计算量下，其探索效率远高于独立重复采样。
*   **纠偏有效性**：实时修正次优动作能显著防止上下文被“污染”，从而提升后续推理的成功率。
*   **模型无关性**：即使使用模型自身的“自批判”能力，也能获得显著的性能提升，证明了该框架的普适性。
*   **性能表现**：在 MuSiQue 等高难度任务上，ProCeedRL 相比标准 DAPO 提升了约 3.72% 的平均准确率；在 ALFWorld 任务中，结合 SFT 后性能提升超过 10%。

### 7. 优点
*   **主动性**：将强化学习中的探索从“碰运气”变成了“有指导的尝试”，解决了长程任务中反馈稀疏且易错的问题。
*   **内化能力**：通过训练将批判器的知识内化到模型中，测试时无需额外的批判器或复杂的推理管线。
*   **效率高**：虽然单次采样变贵了，但达到相同准确率所需的总样本数和计算量反而更少。

### 8. 不足与局限
*   **计算开销**：训练阶段的 Rollout 成本增加，对于算力受限的场景可能存在压力。
*   **批判器依赖**：如果批判器本身给出错误的评分或反馈，可能会引导模型进入错误的优化方向（尽管实验显示其具有一定的鲁棒性）。
*   **理论保证**：目前主要基于经验性的实验结果，缺乏关于“干预频率”与“收敛稳定性”之间关系的严格理论证明。
*   **应用限制**：在不可逆的环境中（如某些实时物理交互），“回溯（Rewind）”操作可能难以实现。

（完）
