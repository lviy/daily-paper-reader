---
title: "TRACE: Capability-Targeted Agentic Training"
title_zh: TRACE：面向能力的针对性智能体训练
authors: "Hangoo Kang, Tarun Suresh, Jon Saad-Falcon, Azalia Mirhoseini"
date: 2026-04-07
pdf: "https://arxiv.org/pdf/2604.05336v1"
tags: ["query:ag-rl"]
score: 9.5
evidence: 针对特定环境的智能体自我提升与训练的端到端系统
tldr: TRACE是一个针对智能体环境的自我改进系统。它通过对比成功与失败的轨迹，自动识别模型缺失的关键能力，并为每种能力生成特定的合成训练环境。通过在这些环境中利用强化学习训练LoRA适配器，并在推理时进行路由，TRACE显著提升了LLM在复杂任务（如客户服务和工具使用）中的表现，且比现有基线方法更具扩展效率。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.05336v1/fig-001.webp\", \"caption\": \"Figure 4: Pass rate on τ2-Bench and ToolSandBox with scaling the number of rollouts.\", \"page\": 10, \"index\": 1, \"width\": 829, \"height\": 304}, {\"url\": \"assets/figures/arxiv/2604.05336v1/fig-002.webp\", \"caption\": \"Figure 1: Overview of TRACE, an end-to-end system for automated environment-specific agent self-improvement. TRACE automatically identifies the specific capabilities that an agent lacks and synthesizes targeted training environments for each capability.\", \"page\": 2, \"index\": 2, \"width\": 748, \"height\": 348}]"
motivation: 现有的智能体训练方法往往依赖非针对性的合成数据或直接在目标环境中训练，难以有效解决模型在特定任务中的具体能力缺陷。
method: TRACE通过对比失败轨迹识别缺失能力，自动构建针对性的合成训练环境，并利用强化学习训练多个LoRA适配器进行推理路由。
result: 在τ²-bench和ToolSandbox基准测试中，TRACE分别比基础模型提升了14.1分和7个满分，性能显著优于GRPO和GEPA等强基线方法。
conclusion: TRACE证明了针对特定能力缺陷进行合成环境构建和强化学习训练是提升智能体性能的高效且可扩展的路径。
---

## 摘要
部署在智能体环境中的大语言模型（LLMs）必须在不同的任务实例中运用多种能力，其中“能力”被定义为在轨迹中执行一个或多个动作，这些动作对于成功解决环境中的子集任务至关重要。许多现有方法要么依赖于并非针对模型在目标环境中实际能力缺陷的合成训练数据，要么直接在目标环境上进行训练，导致模型需要跨任务隐式地学习这些能力。我们提出了 TRACE（将智能体循环失败转化为面向能力的训练环境），这是一个用于特定环境智能体自我提升的端到端系统。TRACE 通过对比成功与失败的轨迹来自动识别缺失的能力，并为每种能力合成一个针对性的训练环境，根据是否运用了该能力提供奖励，随后在每个合成环境上通过强化学习（RL）训练 LoRA 适配器，并在推理时路由至相关的适配器。实验结果表明，TRACE 在不同环境中具有良好的泛化性，在 $τ^2$-bench（客服场景）上比基础智能体提升了 14.1 分，在 ToolSandbox（工具使用场景）上增加了 7 个满分，分别比最强基准模型高出 7.4 分和 4 个满分。在相同数量的采样（rollouts）下，TRACE 的扩展效率优于基准模型，在 $τ^2$-bench 上分别超过 GRPO 和 GEPA 9.2 分和 7.4 分。

## Abstract
Large Language Models (LLMs) deployed in agentic environments must exercise multiple capabilities across different task instances, where a capability is performing one or more actions in a trajectory that are necessary for successfully solving a subset of tasks in the environment. Many existing approaches either rely on synthetic training data that is not targeted to the model's actual capability deficits in the target environment or train directly on the target environment, where the model needs to implicitly learn the capabilities across tasks. We introduce TRACE (Turning Recurrent Agent failures into Capability-targeted training Environments), an end-to-end system for environment-specific agent self-improvement. TRACE contrasts successful and failed trajectories to automatically identify lacking capabilities, synthesizes a targeted training environment for each that rewards whether the capability was exercised, and trains a LoRA adapter via RL on each synthetic environment, routing to the relevant adapter at inference. Empirically, TRACE generalizes across different environments, improving over the base agent by +14.1 points on $τ^2$-bench (customer service) and +7 perfect scores on ToolSandbox (tool use), outperforming the strongest baseline by +7.4 points and +4 perfect scores, respectively. Given the same number of rollouts, TRACE scales more efficiently than baselines, outperforming GRPO and GEPA by +9.2 and +7.4 points on $τ^2$-bench.

---

## 论文详细总结（自动生成）

这篇论文介绍了一个名为 **TRACE**（Turning Recurrent Agent failures into Capability-targeted training Environments）的端到端系统，旨在通过针对性地识别和修复大语言模型（LLM）智能体在特定环境中的能力缺陷，实现自我进化。

以下是对该论文的结构化总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **研究动机**：LLM 智能体在复杂任务（如客服、编程）中往往需要多种核心能力（如数据检索、逻辑推理）。现有的提升方法（如直接在目标环境进行强化学习 RL 或使用通用合成数据）存在**信号稀疏**和**缺乏针对性**的问题。模型很难从最终的成败结果中推断出究竟是哪种底层能力的缺失导致了失败。
*   **核心问题**：如何自动诊断智能体在特定环境中的具体能力短板，并高效地通过训练补齐这些短板？

### 2. 方法论：核心思想与技术流程
TRACE 采用“诊断-合成-训练-路由”的闭环架构：
*   **对比能力识别（Contrastive Identification）**：通过对比成功和失败的轨迹，利用 LLM 分析智能体在哪些关键步骤上表现不一。系统会计算“对比差距”（Contrastive Gap），优先选择那些在失败案例中频繁缺失但在成功案例中存在的特定能力。
*   **针对性环境合成（Environment Synthesis）**：为每种识别出的能力缺陷自动生成一个专门的合成训练环境。这些环境保持与目标环境一致的工具接口和协议，但任务设计高度聚焦于单一能力的锻炼，并提供密集的、可验证的奖励信号。
*   **能力获取（RL Training）**：在每个合成环境上，使用 **GRPO**（Group Relative Policy Optimization）算法训练一个独立的 **LoRA 适配器**。GRPO 通过组内相对奖励来归一化信号，使训练对不同环境的奖励尺度具有鲁棒性。
*   **能力组合与路由（Routing）**：在推理阶段，基础模型充当路由器，根据任务描述和示例判断当前任务最需要哪种能力，并动态激活对应的 LoRA 适配器。

### 3. 实验设计
*   **数据集/场景**：
    *   **$\tau^2$-Bench**：模拟复杂的真实世界客服场景（航空和零售领域），涉及多轮对话和工具调用。
    *   **ToolSandBox**：一个状态化的、对话式的工具使用基准测试。
*   **对比方法**：
    *   **直接 GRPO**：在目标环境直接进行强化学习。
    *   **ADP (Agent Data Protocol)**：在多样化的公开智能体轨迹上进行监督微调（SFT）。
    *   **AWM (Agent World Model)**：使用通用的、非针对性的合成环境进行 RL。
    *   **GEPA**：一种基于进化算法的提示词（Prompt）优化方法。
    *   **模型合并方法**：如 CORE-TSV 等将多个 LoRA 合并为一个的技术。

### 4. 资源与算力
*   **基础模型**：使用 Qwen3-30B-A3B-Instruct 作为基础智能体和模拟器。
*   **硬件资源**：实验使用了 **4 到 8 张 NVIDIA A100-80GB GPU**。
*   **训练细节**：每个能力适配器更新约 5.3% 的参数，使用 AdamW 优化器，学习率为 $10^{-5}$，每个能力训练最多 40 个迭代。

### 5. 实验数量与充分性
*   **实验规模**：论文在两个主流基准测试上进行了详尽的测试，并进行了 10 次独立运行以验证能力识别的稳定性。
*   **消融实验**：对比了“路由模式”与“模型合并/蒸馏模式”的效果，验证了路由在保持多能力互不干扰方面的优越性。
*   **扩展性分析**：分析了性能随采样数量（Rollouts）和能力数量增加的变化曲线。
*   **客观性**：实验设置了严格的 Rollout 预算限制，确保 TRACE 与基线方法在相同算力/数据消耗下进行公平对比。

### 6. 主要结论与发现
*   **性能显著提升**：在 $\tau^2$-Bench 上，TRACE 比基础模型提升了 **14.1%** 的成功率，比最强基线高出 7.4%。在 ToolSandBox 上，完美解决任务的数量增加了 7 个。
*   **扩展效率极高**：在相同的采样数据量下，TRACE 的性能提升斜率远高于直接 RL 或提示词优化方法。
*   **训练优于提示词**：实验证明，仅仅在提示词中加入能力说明（GEPA）是不够的，通过 RL 显式训练模型执行这些能力具有不可替代的作用。
*   **路由优于合并**：将不同能力的 LoRA 独立路由比将其合并到一起效果更好，避免了参数干扰。

### 7. 优点
*   **精准诊断**：通过对比分析自动定位“高影响力”的能力缺陷，避免了盲目训练。
*   **数据效率**：合成环境提供了密集的奖励，使得模型能用极少量的样本学会复杂技能。
*   **模块化**：LoRA 路由机制使得系统可以轻松扩展新能力，而无需重新训练整个模型。

### 8. 不足与局限
*   **路由开销**：推理时需要先进行一次分类判断，虽然开销较小，但在极高并发场景下仍需考虑。
*   **依赖分析器**：系统初期依赖 LLM 分析器对轨迹的解读，如果分析器本身存在严重偏见，可能会识别出错误的能力需求。
*   **任务独立性假设**：目前的路由机制假设一个任务主要由一种核心能力主导，对于需要多种能力深度交织的极复杂任务，简单的路由可能不如更复杂的混合专家（MoE）架构。

（完）
