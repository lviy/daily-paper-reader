---
title: "TTVS: Boosting Self-Exploring Reinforcement Learning via Test-time Variational Synthesis"
title_zh: TTVS：通过测试时变分合成增强自探索强化学习
authors: "Sikai Bai, Haoxi Li, Jie Zhang, Yongjiang Liu, Song Guo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08468v1"
tags: ["query:ag-rl"]
score: 9.5
evidence: 通过强化学习和测试时自适应实现自我进化智能体
tldr: 针对大推理模型（LRM）在缺乏标注数据的领域难以进行强化学习的问题，本文提出TTVS框架。该框架通过测试时变分合成实现自我进化，利用在线变分合成将静态测试查询转化为动态语义等价流，并结合混合探索策略。实验证明，TTVS在仅使用无标注数据的情况下，性能超越了现有的测试时自适应方法及部分高性能有监督强化学习技术，显著提升了模型的逻辑推理能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08468v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison of TTVS with state-of-the-art methods on various model architectures. For RL Post-trained Models w / Labeled Data (·), (·) indicates the amount of labeled data used during post-training.\", \"page\": 6, \"index\": 1, \"width\": 814, \"height\": 902}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-002.webp\", \"caption\": \"Figure 4: Computational cost during test-time training on Qwen3-4B\", \"page\": 8, \"index\": 2, \"width\": 462, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-003.webp\", \"caption\": \"Figure 3: Comparative performance analysis across the five difficulty levels of MATH-500.\", \"page\": 8, \"index\": 3, \"width\": 732, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-004.webp\", \"caption\": \"Figure 1: The schematic illustration of the Test-time Variational Synthesis (TTVS). In Stage 1: Online Variational Synthesis. Pseudo labels are generated from test data to synthesize diversified samples, which are then online filtered for quality and diversity. In Stage 2: Test-time Hybrid Exploration. A policy model is optimized on this augmented data through a hybrid intra- and cross-group exploration strategy, culminating in diversified rewards that guide the model’s optimization.\", \"page\": 4, \"index\": 4, \"width\": 826, \"height\": 588}]"
motivation: 现有的强化学习推理模型高度依赖昂贵的验证奖励，且在测试时容易过拟合静态查询的文本模式，限制了其在未知领域的泛化能力。
method: 提出TTVS框架，通过在线变分合成模块动态生成语义等价的查询流，并结合测试时混合探索策略来平衡准确性与逻辑一致性。
result: 在八种模型架构上的实验表明，TTVS在仅使用无标注测试数据的情况下，性能优于现有的测试时自适应方法及大规模有监督强化学习技术。
conclusion: TTVS证明了通过动态合成与测试时自适应，大推理模型可以在无需外部监督的情况下实现高效的逻辑推理自我进化。
---

## 摘要
尽管由可验证奖励强化学习（RLVR）驱动的大型推理模型（LRMs）取得了显著进展，但该范式在专业或新颖领域受到根本限制，因为在这些领域中，此类监督信号极其昂贵或难以获取，这为测试时自适应带来了关键挑战。虽然现有的测试时方法提供了一种潜在的解决方案，但它们受限于从静态查询集中学习，存在过拟合文本模式的风险。为了填补这一空白，我们引入了测试时变分合成（TTVS），这是一个新颖的框架，通过从无标签测试查询中动态增强训练流，使大型推理模型能够实现自我进化。TTVS 包含两个协同模块：(1) 在线变分合成，它将静态测试查询转化为多样化且语义等价的变体动态流，迫使模型学习底层的逻辑问题而非表层模式；(2) 测试时混合探索，它在合成变体之间平衡了准确性驱动的利用与一致性驱动的探索。广泛的实验表明，TTVS 在八种模型架构上均表现出优异的性能。值得注意的是，仅使用无标签的测试时数据，TTVS 不仅超越了其他测试时自适应方法，还优于在海量高质量有标签数据上训练的最先进的有监督强化学习技术。

## Abstract
Despite significant advances in Large Reasoning Models (LRMs) driven by reinforcement learning with verifiable rewards (RLVR), this paradigm is fundamentally limited in specialized or novel domains where such supervision is prohibitively expensive or unavailable, posing a key challenge for test-time adaptation. While existing test-time methods offer a potential solution, they are constrained by learning from static query sets, risking overfitting to textual patterns. To address this gap, we introduce Test-Time Variational Synthesis (TTVS), a novel framework that enables LRMs to self-evolve by dynamically augmenting the training stream from unlabeled test queries. TTVS comprises two synergistic modules: (1) Online Variational Synthesis, which transforms static test queries into a dynamic stream of diverse, semantically-equivalent variations, enforcing the model to learn underlying problem logic rather than superficial patterns; (2) Test-time Hybrid Exploration, which balances accuracy-driven exploitation with consistency-driven exploration across synthetic variants. Extensive experiments show TTVS yields superior performance across eight model architectures. Notably, using only unlabeled test-time data, TTVS not only surpasses other test-time adaptation methods but also outperforms state-of-the-art supervised RL-based techniques trained on vast, high-quality labeled data.

---

## 论文详细总结（自动生成）

这是一份关于论文《TTVS: Boosting Self-Exploring Reinforcement Learning via Test-time Variational Synthesis》的结构化深入分析报告：

### 1. 核心问题与整体含义
*   **研究动机**：当前大推理模型（LRM）的进步高度依赖于“带可验证奖励的强化学习”（RLVR），但这需要大量高质量的标注数据或规则。在医疗、航天等专业领域，获取此类标注极其昂贵。
*   **核心挑战**：现有的“测试时自适应”（Test-time Adaptation）方法通常在静态测试集上运行，模型容易过拟合特定的文本模式，而非掌握底层的逻辑。
*   **整体含义**：论文提出了 **TTVS（测试时变分合成）** 框架，旨在让模型在面对无标签的测试数据时，通过动态生成问题的变体并进行自我强化，实现“自我进化”，从而在无需外部监督的情况下提升推理能力。

### 2. 方法论：TTVS 框架
TTVS 包含两个核心协同模块，旨在将静态查询转化为动态学习流：

*   **在线变分合成 (Online Variational Synthesis)**：
    1.  **伪标签生成**：对原始测试查询进行多次采样（Rollouts），通过“多数投票”确定共识答案作为伪标签。
    2.  **变体生成**：利用模型自身将原始问题改写为 $k$ 个语义等价但表述不同的新问题。
    3.  **在线过滤**：设置难度脚手架（准确率在 0.125 到 0.875 之间）和长度限制，剔除太简单、太难或格式错误的合成数据。
*   **测试时混合探索 (Test-time Hybrid Exploration)**：
    1.  **组内探索 (IGE)**：针对每个具体查询（原始或合成）进行独立的强化学习更新，侧重于提高特定问题的准确性（利用）。
    2.  **跨组探索 (CGE)**：将原始问题及其变体的解题过程混合，进行统一的多数投票和奖励计算，强制模型在不同表述下保持逻辑一致性（探索与正则化）。
*   **算法基础**：该框架主要基于 **GRPO（组相对策略优化）**，通过组内奖励的均值和标准差来估计优势函数，无需额外的 Critic 模型。

### 3. 实验设计
*   **数据集**：涵盖了高难度的数学与科学推理任务，包括 **MATH-500**、**AIME-2024**、**AMC-2023** 以及研究生水平的 **GPQA**。
*   **Benchmark 模型**：在三个模型家族的 8 种架构上进行了验证：
    *   Qwen3 (1.7B, 4B, 8B)
    *   Qwen2.5 (1.5B, 3B, 7B)
    *   LLaMA (3.2-3B, 3.1-8B)
*   **对比方法**：
    *   **有监督 RL 模型**：如 DeepSeek-R1-Distill (1.5B/7B)、OpenReasoner-Zero 等（这些模型使用了数十万条标注数据）。
    *   **测试时 RL 基准**：TTRL（仅在原始无标签数据上进行多数投票 RL）。

### 4. 资源与算力
*   **硬件环境**：实验使用了 **8 张 NVIDIA H800 (80GB) GPU**。
*   **训练细节**：采用 AdamW 优化器，峰值学习率为 $5 \times 10^{-7}$。
*   **采样规模**：训练时每题采样 32 个响应（Rollouts），推理时采样 16 个并评估 Pass@1。
*   **训练时长**：根据数据集大小设置了 10 到 40 个 Episode 不等。

### 5. 实验数量与充分性
*   **实验规模**：论文在 8 种不同规模和家族的模型上运行，证明了方法的普适性。
*   **消融实验**：非常充分。分别验证了 IGE 和 CGE 模块的贡献、不同 RL 算法（GRPO, OPO, DAPO）的兼容性、过滤阈值的影响、以及采样数量（Rollouts）的敏感性。
*   **客观性**：作者特别对比了“增加采样数”的基线（如 TTRL 使用 64 个 Rollouts 对比 TTVS 使用 32 个），证明了性能提升源于变分合成机制，而非单纯的算力堆砌。此外，还针对“数据污染”在全新的 DeepMath-103K 数据集上进行了验证。

### 6. 主要结论
*   **性能飞跃**：TTVS 在所有测试模型上均显著优于 TTRL。例如在 Qwen3-4B 上，平均分提升了约 30%。
*   **超越有监督**：仅使用无标签测试数据，TTVS 处理后的 Qwen2.5-1.5B 性能甚至超过了在 80 万条标注数据上训练的 DeepSeek-R1-Distill-7B。
*   **难度鲁棒性**：问题越难（如 MATH Level 5），TTVS 相比于传统方法的领先优势越明显，说明变分合成有效弥补了模型的知识盲区。

### 7. 优点
*   **打破静态限制**：通过自改写将有限的测试题扩展为无限的训练流，缓解了测试时过拟合问题。
*   **逻辑一致性**：CGE 机制迫使模型在面对不同问法时给出一致逻辑，增强了推理的鲁棒性。
*   **高效率**：在不显著增加 GPU 显存和推理延迟的前提下，实现了巨大的性能增益。
*   **算法无关性**：可以灵活嵌入 GRPO、DAPO 等多种主流 RL 算法中。

### 8. 不足与局限
*   **模型规模覆盖**：实验主要集中在 1.5B 到 8B 参数的模型，尚未在 32B 或 70B 以上的超大规模模型上验证其扩展性。
*   **模态局限**：目前仅限于纯文本推理，未涉及多模态（视觉-语言）任务。
*   **非客观任务限制**：该方法依赖于“多数投票”产生伪标签，这要求任务必须有客观一致的答案（如数学、编程）。对于创意写作、主观摘要等“非可验证”场景，该框架的适用性尚待研究。

（完）
