---
title: Value-Guidance MeanFlow for Offline Multi-Agent Reinforcement Learning
title_zh: 离线多智能体强化学习中的价值引导 MeanFlow
authors: "Teng Pang, Zhiqiang Dong, Yan Zhang, Rongjian Xu, Guoqiang Wu, Yilong Yin"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08174v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 用于离线多智能体强化学习的价值引导多智能体MeanFlow策略
tldr: 针对离线多智能体强化学习中生成式策略采样效率低且对正则化系数敏感的问题，本文提出VGM²P框架。该方法利用全局优势值引导智能体协作，将最优策略学习转化为条件行为克隆，并结合MeanFlow模型实现高效动作生成。实验证明，该方法在离散和连续任务中均能以极高的推理效率达到SOTA性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08174v1/fig-001.webp\", \"caption\": \"Figure 5: The training curve for SMAC.\", \"page\": 22, \"index\": 1, \"width\": 701, \"height\": 559}, {\"url\": \"assets/figures/arxiv/2604.08174v1/fig-002.webp\", \"caption\": \"Figure 1: The training curve between different BC and VGM2P.\", \"page\": 10, \"index\": 2, \"width\": 686, \"height\": 610}, {\"url\": \"assets/figures/arxiv/2604.08174v1/fig-003.webp\", \"caption\": \"Figure 4: Comparison of running time (minutes). These results are the averages across different tasks in each environment.\", \"page\": 14, \"index\": 3, \"width\": 545, \"height\": 381}, {\"url\": \"assets/figures/arxiv/2604.08174v1/fig-004.webp\", \"caption\": \"Figure 6: The training curve for MA-MuJoCo.\", \"page\": 23, \"index\": 4, \"width\": 547, \"height\": 603}, {\"url\": \"assets/figures/arxiv/2604.08174v1/fig-005.webp\", \"caption\": \"Figure 3: The training curve for different guidance weights.\", \"page\": 13, \"index\": 5, \"width\": 686, \"height\": 610}, {\"url\": \"assets/figures/arxiv/2604.08174v1/fig-006.webp\", \"caption\": \"Table 3: Hyperparameter for Meanflow model\", \"page\": 19, \"index\": 6, \"width\": 612, \"height\": 243}, {\"url\": \"assets/figures/arxiv/2604.08174v1/fig-007.webp\", \"caption\": \"Figure 2: The training curve for different Q-value training methods of 6HalfCheetah scenarios in MA-MuJoCo.\", \"page\": 12, \"index\": 7, \"width\": 516, \"height\": 586}, {\"url\": \"assets/figures/arxiv/2604.08174v1/fig-008.webp\", \"caption\": \"Table 1: Comparative performance of VGM2P with discrete actions environment. For the SMACv1 environment, we select 4 tasks, each with 3 datasets of varying quality. For the SMACv2 environment, we select three tasks with only 1 dataset. To distinguish different Behavior Cloning methods and simplify notation, we use FM and MF to represent Flow Matching and MeanFlow, respectively. We report the average performances and standard deviations of each algorithm across 6 seeds, with the best result in bold and the second-best result underlined.\", \"page\": 9, \"index\": 8, \"width\": 714, \"height\": 287}, {\"url\": \"assets/figures/arxiv/2604.08174v1/fig-009.webp\", \"caption\": \"Table 2: Comparative performance of VGM2P with continuous actions environment. For the MA-MuJoCo environment, we select 3 tasks, each with 4 datasets of varying quality. For simplicity, we use ME and MR to represent Medium-Expert and Medium-Replay, respectively.\", \"page\": 9, \"index\": 9, \"width\": 731, \"height\": 238}]"
motivation: 现有的基于扩散或流模型的离线多智能体策略存在采样效率低下以及对行为正则化系数过于敏感的问题。
method: 提出VGM²P框架，利用全局优势值引导协作，并采用无分类器引导的MeanFlow进行高效的条件行为克隆。
result: 在多种离散和连续动作空间任务中，VGM²P仅通过条件行为克隆就实现了与当前最先进方法相当的性能。
conclusion: VGM²P是一种简单高效的流式策略学习框架，在保证多智能体协作表达能力的同时显著提升了推理效率。
---

## 摘要
离线多智能体强化学习 (MARL) 旨在从预先收集的数据集中学习最优联合策略，这需要在最大化全局收益与缓解离线数据带来的分布偏移之间进行权衡。近期的研究使用扩散或流生成模型来捕捉智能体之间复杂的联合策略行为；然而，它们通常依赖多步迭代采样，从而降低了训练和推理效率。尽管后续研究通过蒸馏等方法提高了采样效率，但其对行为正则化系数仍然敏感。为了解决上述问题，我们提出了价值引导多智能体 MeanFlow 策略 (VGM$^2$P)，这是一个简单且有效的基于流的策略学习框架，能够通过对系数不敏感的条件行为克隆实现高效的动作生成。具体而言，VGM$^2$P 使用全局优势值来引导智能体协作，将最优策略学习视为条件行为克隆。此外，为了提高多智能体场景下的策略表达能力和推理效率，它在策略训练和执行中均采用了无分类器引导 (classifier-free guidance) 的 MeanFlow。在离散和连续动作空间任务上的实验表明，即使仅通过条件行为克隆进行训练，VGM$^2$P 也能高效地达到与当前最先进方法相当的性能。

## Abstract
Offline multi-agent reinforcement learning (MARL) aims to learn the optimal joint policy from pre-collected datasets, requiring a trade-off between maximizing global returns and mitigating distribution shift from offline data. Recent studies use diffusion or flow generative models to capture complex joint policy behaviors among agents; however, they typically rely on multi-step iterative sampling, thereby reducing training and inference efficiency. Although further research improves sampling efficiency through methods like distillation, it remains sensitive to the behavior regularization coefficient. To address the above-mentioned issues, we propose Value Guidance Multi-agent MeanFlow Policy (VGM$^2$P), a simple yet effective flow-based policy learning framework that enables efficient action generation with coefficient-insensitive conditional behavior cloning. Specifically, VGM$^2$P uses global advantage values to guide agent collaboration, treating optimal policy learning as conditional behavior cloning. Additionally, to improve policy expressiveness and inference efficiency in multi-agent scenarios, it leverages classifier-free guidance MeanFlow for both policy training and execution. Experiments on tasks with both discrete and continuous action spaces demonstrate that, even when trained solely via conditional behavior cloning, VGM$^2$P efficiently achieves performance comparable to state-of-the-art methods.

---

## 论文详细总结（自动生成）

这是一份关于论文《Value-Guidance MeanFlow for Offline Multi-Agent Reinforcement Learning》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
离线多智能体强化学习（Offline MARL）面临两大核心挑战：一是**分布偏移（Distribution Shift）**，即智能体在训练中容易接触到离线数据集之外的动作，导致外推误差；二是**多智能体协作的复杂性**，需要捕捉智能体间高度非线性的联合策略。
虽然近期基于扩散模型（Diffusion）或流匹配（Flow Matching）的方法提升了策略的表达能力，但存在**采样效率低下**（需多步迭代）和**对正则化系数高度敏感**的问题。本研究旨在提出一种既能保持高表达能力，又能实现单步高效采样，且对超参数鲁棒的离线 MARL 框架。

### 2. 方法论：VGM²P 框架
VGM²P 的核心思想是将最优策略学习转化为**以价值为引导的条件行为克隆（Conditional Behavior Cloning）**。
*   **核心思想**：利用贝叶斯定理，证明了如果行为策略以“优势值（Advantage Value）”为条件进行采样，其分布可以逼近最优策略。
*   **关键技术细节**：
    *   **价值引导（Value Guidance）**：计算全局优势值 $A(o, a) = Q(o, a) - V(o)$。在训练时，若动作的优势值非负，则赋予条件标签 $c=1$（代表优良动作），否则为 $c=0$。
    *   **MeanFlow 策略建模**：采用 MeanFlow 模型（一种改进的流匹配模型）来建模策略。MeanFlow 的优势在于它学习的是平均速度场，训练完成后可以实现**单步（One-step）动作生成**，显著优于扩散模型的多步采样。
    *   **无分类器引导（Classifier-free Guidance, CFG）**：在 MeanFlow 中引入 CFG 技术，通过调节引导权重 $\omega$ 来平衡行为克隆的稳定性与最优值的导向性。
    *   **中心化训练与去中心化执行（CTDE）**：遵循 IGM 原则（个体-全局最大化），通过价值分解（如 VDN 结构）将全局 Q 值分解为个体 Q 值，从而实现智能体间的协作引导。

### 3. 实验设计
*   **实验场景与数据集**：
    *   **SMAC v1 & v2**：星际争霸 II 挑战赛，包含离散动作空间，涵盖 Good、Medium、Poor 及 Replay 等多种质量的数据集。
    *   **MA-MuJoCo**：多智能体机器人控制，包含连续动作空间，涵盖 Expert、Medium-Expert 等数据集。
*   **对比方法（Baselines）**：
    *   **传统离线方法**：MA-BCQ, MA-CQL, ICQ, OMAR, OMIGA。
    *   **生成式模型方法**：MADiff（扩散模型）, DoF（扩散分解）, MAC-Flow（流匹配+蒸馏）。
    *   **基础行为克隆**：基于高斯、扩散、流匹配和 MeanFlow 的普通 BC。

### 4. 资源与算力
*   **硬件环境**：使用了单张 **NVIDIA RTX 3090 (24GB)** GPU。
*   **软件框架**：基于 Python 和 **JAX** 库实现。
*   **训练规模**：SMAC 环境训练步数为 $10^6$ 步，MA-MuJoCo 为 $5 \times 10^5$ 步。
*   **效率表现**：文中对比了运行时间，VGM²P 在 SMACv1 上约需 112 分钟，显著快于需要多步采样的模型，与经过蒸馏的 MAC-Flow 效率相当。

### 5. 实验数量与充分性
*   **实验规模**：在 3 个大类基准环境、超过 15 个具体任务/数据集组合上进行了测试。
*   **重复性**：每个实验均使用 **6 个随机种子**运行，并报告了平均值和标准差。
*   **消融实验**：针对 IGM 原则的有效性、引导权重 $\omega$ 的敏感度、以及不同生成模型的运行效率进行了详细的消融分析。
*   **评价**：实验设计较为充分，涵盖了离散与连续空间，对比了从传统到前沿的各类算法，结果具有较强的说服力和客观性。

### 6. 主要结论与发现
*   **性能优异**：VGM²P 仅通过条件行为克隆，就在大多数任务中达到了与 SOTA（如 OMIGA, MAC-Flow）相当甚至更优的水平。
*   **高效采样**：得益于 MeanFlow 架构，该方法在保持高性能的同时，实现了单步推理，大幅降低了计算开销。
*   **鲁棒性**：实验证明 VGM²P 对引导系数 $\omega$ 不敏感，在较大范围内都能保持稳定性能，降低了调参难度。
*   **协作有效性**：基于 IGM 的价值分解引导比独立的个体价值引导更能挖掘全局最优策略。

### 7. 优点（亮点）
*   **简洁高效**：避开了复杂的扩散模型多步采样和繁琐的策略蒸馏过程，直接利用 MeanFlow 实现单步生成。
*   **理论支撑**：通过数学证明（Proposition 1 & 2）建立了条件行为克隆与最优策略之间的联系。
*   **通用性强**：一套框架同时适用于离散和连续动作空间，且在不同质量的数据集上表现稳健。

### 8. 不足与局限
*   **复杂场景泛化有限**：在 SMAC v2（更具随机性和复杂性）的 Replay 数据集上，表现虽优于普通 BC，但相比专门针对该环境优化的算法仍有提升空间。
*   **依赖数据质量**：虽然引入了价值引导，但本质仍属于行为克隆范畴，如果离线数据中完全缺乏高奖励轨迹，模型上限会受限。
*   **协作机制单一**：目前主要依赖价值分解进行协作引导，未来可探索更复杂的通信或图神经网络机制来增强智能体间的互动。

（完）
