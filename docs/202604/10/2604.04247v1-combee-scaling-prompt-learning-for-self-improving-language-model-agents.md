---
title: "Combee: Scaling Prompt Learning for Self-Improving Language Model Agents"
title_zh: Combee：面向自我改进语言模型智能体的大规模并行提示学习
authors: "Hanchen Li, Runyuan He, Qizheng Zhang, Changxiu Ji, Qiuyang Mang, Xiaokun Chen, Lakshya A Agrawal, Wei-Liang Liao, Eric Yang, Alvin Cheung, James Zou, Kunle Olukotun, Ion Stoica, Joseph E. Gonzalez"
date: 2026-04-05
pdf: "https://arxiv.org/pdf/2604.04247v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 通过从智能体轨迹中并行提示学习实现自我改进的语言模型智能体
tldr: 针对现有大模型智能体提示词学习方法在处理大规模轨迹时效率低且并行扩展性差的问题，本文提出Combee框架。该框架通过并行扫描、增强洗牌机制和动态批大小控制，实现了高效的并行提示词学习。实验表明，Combee在保持或提升准确率的同时，比现有方法提速高达17倍，有效支持了自我进化智能体的大规模并行训练。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.04247v1/fig-001.webp\", \"caption\": \"Figure 2: Context overload from naive scaling. As batch size increases, the aggregator LLM produces monotonically fewer and less useful context updates, directly degrading final accuracy across benchmarks.\", \"page\": 3, \"index\": 1, \"width\": 739, \"height\": 210}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-002.webp\", \"caption\": \"Figure 4: Combee achieves superior quality-delay trade off on GEPA for finance benchmarks.\", \"page\": 8, \"index\": 2, \"width\": 822, \"height\": 252}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-003.webp\", \"caption\": \"Figure 5: Combee achieves superior quality-delay trade off on ACE for finance benchmarks.\", \"page\": 8, \"index\": 3, \"width\": 797, \"height\": 258}, {\"url\": \"assets/figures/arxiv/2604.04247v1/fig-004.webp\", \"caption\": \"Figure 3: Overall design of Combee (top) vs. naive scaling (bottom). Combee follows a Map-Shuffle-Reduce paradigm: the Map phase dispatches n parallel agents to execute queries and reflect; the Shuffle phase applies augmented shuffling; and the Reduce phase hierarchically combines reflections via parallel scan aggregation. In contrast, naive scaling feeds all reflections directly into a single prompt update, causing context overload.\", \"page\": 5, \"index\": 4, \"width\": 732, \"height\": 552}]"
motivation: 现有的提示词学习方法在处理大量智能体运行轨迹时缺乏高效的并行扩展能力，且在高并行度下往往会出现性能下降。
method: 提出Combee框架，利用并行扫描、增强洗牌机制以及动态批大小控制器来平衡学习质量与处理延迟。
result: 在AppWorld和Terminal-Bench等多个基准测试中，Combee在保证准确率和成本的前提下，实现了最高17倍的加速。
conclusion: Combee成功解决了自我进化智能体在提示词学习中的扩展性难题，为大规模并行智能体系统的优化提供了高效且高质量的方案。
---

## 摘要
提示学习（prompt learning）的最新进展使得大语言模型智能体能够在不改变参数的情况下，从推理时上下文中获取任务相关知识。例如，现有方法（如 ACE 或 GEPA）可以根据之前的智能体运行记录学习系统提示词，从而提高准确率。然而，这些方法主要关注单智能体或低并行度的设置，这从根本上限制了它们从大量收集的智能体轨迹（agentic traces）中高效学习的能力。为了适应从海量智能体轨迹或并行智能体执行中学习的日益增长的趋势，并行运行提示学习将是高效且有益的。然而，由于缺乏原则性的扩展策略，现有方法在高并行度下会出现质量下降。为了同时提高提示学习的效率和质量，我们提出了 Combee，这是一个为自我改进智能体扩展并行提示学习的新型框架。Combee 加速了学习过程，并支持在并行运行多个智能体的同时，从它们的聚合轨迹中学习且不损失质量。为实现这一目标，Combee 利用了并行扫描（parallel scans）并采用了增强的洗牌机制（augmented shuffle mechanism）；此外，Combee 还引入了动态批大小控制器以平衡质量和延迟。在 AppWorld、Terminal-Bench、Formula 和 FiNER 上的评估表明，Combee 在保持相当或更高准确率以及同等成本的前提下，比之前的方法实现了高达 17 倍的加速。

## Abstract
Recent advances in prompt learning allow large language model agents to acquire task-relevant knowledge from inference-time context without parameter changes. For example, existing methods (like ACE or GEPA) can learn system prompts to improve accuracy based on previous agent runs. However, these methods primarily focus on single-agent or low-parallelism settings. This fundamentally limits their ability to efficiently learn from a large set of collected agentic traces. It would be efficient and beneficial to run prompt learning in parallel to accommodate the growing trend of learning from many agentic traces or parallel agent executions. Yet without a principled strategy for scaling, current methods suffer from quality degradation with high parallelism. To improve both the efficiency and quality of prompt learning, we propose Combee, a novel framework to scale parallel prompt learning for self-improving agents. Combee speeds up learning and enables running many agents in parallel while learning from their aggregate traces without quality degradation. To achieve this, Combee leverages parallel scans and employs an augmented shuffle mechanism; Combee also introduces a dynamic batch size controller to balance quality and delay. Evaluations on AppWorld, Terminal-Bench, Formula, and FiNER demonstrate that Combee achieves up to 17x speedup over previous methods with comparable or better accuracy and equivalent cost.

---

## 论文详细总结（自动生成）

这是一份关于论文《Combee: Scaling Prompt Learning for Self-Improving Language Model Agents》的结构化深入总结：

### 1. 核心问题与整体含义（研究动机和背景）
论文关注的是**大语言模型（LLM）智能体的自我改进能力**。现有的提示学习（Prompt Learning）方法（如 ACE、GEPA）通过“生成-反思-更新”循环，让智能体从推理过程的轨迹中提取知识并更新系统提示词（如 Playbook 或规则库），从而在不微调参数的情况下提升性能。
**研究动机：** 现有的方法大多是串行或低并行度的。当尝试通过增加批大小（Batch Size）来并行化学习过程时，会出现**“上下文过载”（Context Overload）**现象：聚合器 LLM 在面对海量反思（Reflections）时，会倾向于保留泛化的模式而丢弃高价值的特定细节，导致学习质量严重下降。Combee 的目标是在保持学习质量的同时，实现大规模并行提示学习的扩展。

### 2. 方法论：核心思想与关键技术
Combee 采用了 **Map-Shuffle-Reduce** 范式，核心思想是通过分层聚合和冗余处理来克服上下文过载。

*   **并行扫描聚合（Parallel Scan Aggregation）：** 借鉴并行计算中的前缀和思想。不再将所有 $n$ 个轨迹的反思一次性塞给 LLM，而是将其分为 $k$ 个小组（默认 $k = \sqrt{n}$）。先在小组内聚合生成局部更新，再将这些局部更新分层合并。这种分治法确保了每次 LLM 处理的上下文都在其高效处理范围内。
*   **增强洗牌机制（Augmented Shuffling）：** 为了防止关键信息在聚合过程中丢失，Combee 将每个反思信号复制 $p$ 次（默认 $p=2$）并进行随机洗牌。这增加了重要反思被聚合器捕捉到的概率，类似于集成学习中的自洽性（Self-consistency）。
*   **动态批大小控制器（Dynamic Batch Size Controller）：** 自动平衡训练延迟和质量。通过拟合幂律延迟曲线，寻找边际收益递减的临界点，选择既能显著提速又不会因批次过大而导致质量崩溃的最佳批大小。

### 3. 实验设计
*   **数据集/场景：**
    *   **智能体基准：** AppWorld（多步 API 任务）、Terminal-Bench 2.0（命令行软件工程任务）。
    *   **领域特定任务：** FiNER（金融实体识别）、Formula（金融数值推理）。
*   **对比方法：**
    *   **基础模型：** 无提示学习的 Base LLM。
    *   **串行基准：** ACE (Batch Size = 1) 和 GEPA。
    *   **朴素并行：** 直接增加 Batch Size 的 ACE/GEPA。
    *   **其他策略：** Top-K 检索（基于嵌入聚类）、摘要法（Summarization）。

### 4. 资源与算力
*   **模型：** 主要实验使用 **DeepSeek-V3.1**（128K 上下文），部分实验使用了 **GPT-OSS 120B** 以验证跨模型鲁棒性。
*   **算力说明：** 论文未明确列出具体的 GPU 硬件型号（如 H100 数量），但详细记录了**训练时长（秒/分钟）**和**API 成本（美元）**。
*   **效率提升：** 在 Terminal-Bench 上实现了 **17倍** 的加速；在 AppWorld 上实现了 **12倍** 加速，且成本保持一致。

### 5. 实验数量与充分性
*   **实验规模：** 涵盖了 4 个不同性质的基准测试，进行了多组不同 Batch Size（1 到 125）的对比实验。
*   **消融实验：** 专门针对“增强洗牌”和“动态批大小控制器”进行了消融分析，验证了各组件的必要性。
*   **充分性评价：** 实验设计较为充分且公平。作者不仅对比了最终准确率，还通过“Playbook 词元数”和“反思命中率”进行了定性分析，解释了为何 Combee 能保留更多有效知识。跨模型（DeepSeek 和 GPT-OSS）的实验进一步增强了结论的客观性。

### 6. 主要结论与发现
*   **克服过载：** 朴素的并行化会导致 LLM 产生“有损压缩”，丢弃特定任务的微观策略。Combee 通过分层聚合成功保留了这些细节。
*   **性能与速度双赢：** Combee 在高并行度下（如 BS=40 或 100）的准确率显著优于朴素并行方法，甚至在某些任务上超过了耗时极长的串行学习。
*   **框架无关性：** Combee 可以无缝集成到 ACE 和 GEPA 等不同的提示学习框架中。

### 7. 优点
*   **系统性解决扩展性：** 首次系统性地识别并解决了提示学习中的“上下文过载”问题。
*   **高效性：** 极大地缩短了智能体自我进化的迭代周期（从小时级缩短到分钟级）。
*   **即插即用：** 作为一个分布式框架，它不改变底层提示学习的逻辑，具有很强的通用性。

### 8. 不足与局限
*   **同步限制：** 目前的设计假设每一轮迭代内是同步并行执行的，未来若能支持异步更新（类似异步 SGD）可能会进一步提升吞吐量。
*   **超参数依赖：** 动态控制器依赖于预定义的阈值 $\tau$ 和幂律模型，在极端不同的工作负载下可能需要微调。
*   **长程依赖：** 虽然解决了单次聚合的过载，但对于跨越极长周期的知识遗忘问题，文中未做深入探讨。

（完）
