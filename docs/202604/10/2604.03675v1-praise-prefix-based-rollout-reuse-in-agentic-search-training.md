---
title: "PRAISE: Prefix-Based Rollout Reuse in Agentic Search Training"
title_zh: PRAISE：智能体搜索训练中基于前缀的展开重用
authors: "Erhan Zhang, Yiqun Chen, Zechun Niu, Wei Yang, Xiaochi Wei, Yan Gao, Yi Wu, Yao Hu, Jiaxin Mao"
date: 2026-04-04
pdf: "https://arxiv.org/pdf/2604.03675v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 利用强化学习提高智能体搜索训练中数据效率和信用分配的框架
tldr: 针对智能体搜索中长程采样利用率低和奖励稀疏的问题，本文提出了 PRAISE 框架。该方法通过提取搜索轨迹中的前缀状态并生成中间答案，实现了轨迹的复用和步级奖励的自动推导。PRAISE 使用单一模型同时进行策略学习和前缀评估，在多跳问答任务上显著提升了训练效率和模型性能。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.03675v1/fig-001.webp\", \"caption\": \"Figure 3: Effect of the process-reward weight α under different model sizes and evaluation metrics. None denotes the variant without prefix evaluator. The settings 0–1.0 correspond to different values of the process-reward weight α. A larger α assigns a higher weight to the process reward relative to the final reward.\", \"page\": 8, \"index\": 1, \"width\": 950, \"height\": 254}, {\"url\": \"assets/figures/arxiv/2604.03675v1/fig-002.webp\", \"caption\": \"Table 1: Main results on five QA benchmarks. The best result in each column is highlighted in bold. We also report the absolute improvement of PRAISE over the strongest baseline in each column (Impv. vs Best).\", \"page\": 7, \"index\": 2, \"width\": 941, \"height\": 402}, {\"url\": \"assets/figures/arxiv/2604.03675v1/fig-003.webp\", \"caption\": \"Figure 2: Step-wise analysis of the prefix evaluator under different optimization strategies. Panels (a)–(c) show the evaluator’s F1 across training steps for prefixes taken at Turn 0, Turn 1, and Turn 2, respectively. Panel (d) reports the corresponding F1 gain of joint optimization over the strongest non-joint baseline at each turn.\", \"page\": 7, \"index\": 3, \"width\": 950, \"height\": 222}, {\"url\": \"assets/figures/arxiv/2604.03675v1/fig-004.webp\", \"caption\": \"Figure 1: Overview of PRAISE. Left: Main Search Rollout. The policy performs multi-turn search and produces a complete trajectory with a final answer. Middle: Prefix Answering. PRAISE extracts prefix states and generates an intermediate answer from each prefix. Right: Reward Assignment and Joint optimization. Prefix answers are scored against the ground-truth answer, step rewards are computed from adjacent score differences, and the final answer receives a terminal reward. All resulting samples are jointly used for PPO training.\", \"page\": 5, \"index\": 4, \"width\": 927, \"height\": 575}]"
motivation: 现有的搜索强化学习方法存在长程采样成本高、利用率低以及仅有最终奖励导致的奖励稀疏问题。
method: 通过提取轨迹前缀生成中间答案，并利用前缀间的性能差异构建步级奖励，同时复用这些前缀构造额外的训练数据。
result: 在多跳问答基准测试中，PRAISE 相比强基线模型取得了持续的性能提升。
conclusion: PRAISE 证明了通过前缀复用和中间奖励机制，可以在无需额外人工标注或独立奖励模型的情况下，有效优化智能体搜索任务。
---

## 摘要
在智能体搜索中，大语言模型（LLMs）被训练用于执行多轮检索和推理，以处理如多跳问答（QA）等复杂任务。然而，目前的基于搜索的强化学习（RL）方法面临两个核心局限：昂贵的长时程展开（rollouts）在训练中未被充分利用，且监督信号通常仅在最终答案处提供，导致严重的奖励稀疏问题。我们提出了 PRAISE（基于前缀的展开重用及中间步骤奖励的智能体搜索），这是一个旨在提高智能体搜索训练中数据效率和信用分配的框架。给定一条完整的搜索轨迹，PRAISE 提取不同搜索轮次的前缀状态，从中诱导出中间答案，并利用这些前缀来构建额外的训练轨迹，同时根据不同前缀之间的性能差异推导出步骤级奖励。我们的方法使用单一共享模型同时进行搜索策略学习和前缀答案评估，从而在无需额外人工标注或独立奖励模型的情况下实现联合优化。在多跳问答基准测试上的实验表明，PRAISE 相比强基准模型持续提升了性能。

## Abstract
In agentic search, large language models (LLMs) are trained to perform multi-turn retrieval and reasoning for complex tasks such as multi-hop question answering (QA). However, current search-based Reinforcement Learning (RL) methods suffer from two core limitations: expensive long-horizon rollouts are under-utilized during training, and supervision is typically available only at the final answer, resulting in severe reward sparsity. We present Prefix-based Rollout reuse for Agentic search with Intermediate Step rEwards (PRAISE), a framework for improving both data efficiency and credit assignment in agentic search training. Given a complete search trajectory, PRAISE extracts prefix states at different search turns, elicits intermediate answers from them, and uses these prefixes both to construct additional training trajectories and to derive step-level rewards from performance differences across prefixes. Our method uses a single shared model for both search policy learning and prefix answer evaluation, enabling joint optimization without extra human annotations or a separate reward model. Experiments on multi-hop QA benchmarks show that PRAISE consistently improves performance over strong baselines.

---

## 论文详细总结（自动生成）

这是一份关于论文《PRAISE: Prefix-Based Rollout Reuse in Agentic Search Training》的结构化深入分析报告：

### 1. 论文的核心问题与整体含义（研究动机和背景）
论文聚焦于**智能体搜索（Agentic Search）**的训练优化。在处理复杂的多跳问答（Multi-hop QA）时，大语言模型需要通过多轮检索和推理来获取答案。
*   **核心痛点**：
    1.  **数据效率低下**：生成一条长程搜索轨迹（Rollout）成本极高，但现有方法通常只将其作为一个训练样本，忽略了中间状态的价值。
    2.  **奖励稀疏与信用分配困难**：监督信号往往只在轨迹末尾（最终答案）给出。模型很难判断中间哪一次搜索是有益的，哪一次是冗余或有害的。
*   **研究意义**：提出一种无需额外人工标注、无需独立奖励模型的框架，通过复用轨迹前缀和构建中间奖励，提升模型在复杂搜索任务中的学习效率和性能。

### 2. 论文提出的方法论
论文提出了 **PRAISE** 框架，其核心思想是利用搜索轨迹的**天然前缀结构**。

*   **核心思想**：
    *   **前缀展开重用（Rollout Reuse）**：将一条完整的 $T$ 轮搜索轨迹拆解为 $T+1$ 个前缀状态。对于每个前缀，让模型尝试直接给出答案。这些“前缀-答案”对被转化为额外的训练样本。
    *   **中间步骤奖励（Intermediate Step Rewards）**：通过比较相邻两个前缀状态下生成答案的质量差异，计算出该搜索步骤的“边际贡献”，从而将稀疏的终端奖励转化为密集的步级奖励。
*   **关键技术细节**：
    1.  **共享模型设计**：使用同一个模型 $\pi_\theta$ 处于两种模式：**搜索模式**（负责多轮推理和检索）和**答案模式**（负责基于当前信息给出结论）。
    2.  **奖励计算公式**：
        *   前缀得分 $v_t = R(\tilde{y}_t, y)$，其中 $\tilde{y}_t$ 是第 $t$ 轮后的中间答案。
        *   过程奖励 $r_{proc}^t = \alpha(v_t - v_{t-1})$，反映了第 $t$ 轮搜索带来的性能增益。
    3.  **联合优化**：使用 PPO 算法同时优化主搜索轨迹和所有重用的前缀答案轨迹。

### 3. 实验设计
*   **数据集**：
    *   **训练集**：HotpotQA 和 2WikiMultihopQA。
    *   **测试集**：NQ (Natural Questions), HotpotQA, 2WikiMultihopQA, Bamboogle, MuSiQue。
*   **Benchmark 与对比方法**：
    *   **标准基线**：直接回答（Direct LLM）、朴素 RAG。
    *   **智能体搜索基线**：Search-o1、Search-R1、R1-Searcher。
    *   **过程监督基线**：StepSearch（基于参考子问题）、ReasonRAG（基于路径采样）、TIPS（基于信息增益）。

### 4. 资源与算力
*   **模型架构**：基础模型使用 **Qwen2.5-7B**。
*   **框架**：使用开源强化学习框架 **verl**。
*   **检索组件**：使用 E5 嵌入模型和维基百科语料库。
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量及总训练时长。考虑到 7B 模型的 PPO 训练及多轮 Rollout，通常需要 A100 或 H100 等级的计算集群。

### 5. 实验数量与充分性
*   **实验规模**：在 5 个主流多跳问答基准上进行了测试，涵盖了 EM（精确匹配）和 F1 两个核心指标。
*   **消融实验**：非常充分。作者对比了“无联合优化”、“无过程奖励”、“无前缀评估器”等变体，并测试了不同模型规模（7B vs 14B）下的表现。
*   **客观性**：通过设置不同的过程奖励权重 $\alpha$ 进行鲁棒性分析，并展示了训练过程中评估器能力的演进曲线，证明了性能提升并非偶然，而是源于更精准的中间反馈。

### 6. 论文的主要结论与发现
1.  **性能领先**：PRAISE 在所有测试集上均显著优于现有的智能体搜索和过程监督方法。
2.  **联合优化的必要性**：共享模型并进行联合训练的效果远好于使用冻结的外部评估器。这是因为评估器能随策略同步进化，提供更一致的反馈。
3.  **缓解奖励稀疏**：通过相邻前缀的得分差构建的奖励信号，能有效引导模型学习“何时搜索”和“搜索什么”。
4.  **数据效率**：通过前缀复用，单次 Rollout 提取的训练信号量大幅增加，加速了模型收敛。

### 7. 优点（亮点）
*   **自给自足（Self-contained）**：不需要金牌子问题标注，也不需要额外的奖励模型（RM），降低了系统复杂度和标注成本。
*   **优雅的结构复用**：巧妙利用了搜索任务的序列特性，将推理过程中的“废料”转化为高质量的训练数据。
*   **动态对齐**：解决了评估器与策略模型之间的分布偏移问题，使得中间奖励更加可靠。

### 8. 不足与局限
*   **任务局限性**：实验主要集中在知识密集型的 QA 任务上，对于数学推理、代码生成等其他需要 Agent 能力的场景探讨不足。
*   **计算开销**：虽然提高了数据效率，但在训练期间为每个前缀生成中间答案会增加额外的计算负担（尽管推理时无额外开销）。
*   **评估依赖**：该方法依赖于可验证的奖励函数（如 EM/F1），对于答案开放、难以自动评分的任务（如创意写作或开放式调研），其适用性受限。

（完）
