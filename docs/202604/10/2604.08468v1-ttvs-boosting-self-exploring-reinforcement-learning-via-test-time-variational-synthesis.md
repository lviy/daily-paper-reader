---
title: "TTVS: Boosting Self-Exploring Reinforcement Learning via Test-time Variational Synthesis"
title_zh: TTVS：通过测试时变分合成增强自探索强化学习
authors: "Sikai Bai, Haoxi Li, Jie Zhang, Yongjiang Liu, Song Guo"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08468v1"
tags: ["query:ag-rl"]
score: 9.0
evidence: 通过强化学习和变分合成使大型推理模型能够自我进化
tldr: 本研究针对大语言推理模型在缺乏可验证奖励的领域难以进行测试时自适应的问题，提出了TTVS框架。该框架通过在线变分合成将静态测试查询转化为动态的语义等价变体流，强制模型学习底层逻辑而非文本模式，并结合混合探索策略实现自我进化。实验证明，TTVS在仅使用无标签数据的情况下，性能不仅优于现有测试时自适应方法，甚至超越了依赖大量标注数据的监督强化学习技术。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2604.08468v1/fig-001.webp\", \"caption\": \"Table 1: Performance comparison of TTVS with state-of-the-art methods on various model architectures. For RL Post-trained Models w / Labeled Data (·), (·) indicates the amount of labeled data used during post-training.\", \"page\": 6, \"index\": 1, \"width\": 814, \"height\": 902}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-002.webp\", \"caption\": \"Figure 4: Computational cost during test-time training on Qwen3-4B\", \"page\": 8, \"index\": 2, \"width\": 462, \"height\": 280}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-003.webp\", \"caption\": \"Figure 3: Comparative performance analysis across the five difficulty levels of MATH-500.\", \"page\": 8, \"index\": 3, \"width\": 732, \"height\": 237}, {\"url\": \"assets/figures/arxiv/2604.08468v1/fig-004.webp\", \"caption\": \"Figure 1: The schematic illustration of the Test-time Variational Synthesis (TTVS). In Stage 1: Online Variational Synthesis. Pseudo labels are generated from test data to synthesize diversified samples, which are then online filtered for quality and diversity. In Stage 2: Test-time Hybrid Exploration. A policy model is optimized on this augmented data through a hybrid intra- and cross-group exploration strategy, culminating in diversified rewards that guide the model’s optimization.\", \"page\": 4, \"index\": 4, \"width\": 826, \"height\": 588}]"
motivation: 针对推理模型在缺乏标注奖励的领域难以自适应，且容易过拟合静态查询文本模式的局限性。
method: 提出TTVS框架，利用在线变分合成生成动态查询变体，并采用混合探索策略平衡模型的准确性与逻辑一致性。
result: 在八种模型架构上均取得显著提升，仅凭无标签测试数据即超越了最先进的监督强化学习方法。
conclusion: TTVS通过动态合成与自我探索，为推理模型在无监督环境下的持续进化提供了一种高效且强大的新范式。
---

## 摘要
尽管由可验证奖励强化学习（RLVR）驱动的大型推理模型（LRMs）取得了显著进展，但在监督信号极其昂贵或不可用的专业或新颖领域，该范式受到根本性限制，这为测试时自适应带来了关键挑战。虽然现有的测试时方法提供了一种潜在的解决方案，但它们受限于从静态查询集中学习，存在过拟合文本模式的风险。为了填补这一空白，我们引入了测试时变分合成（TTVS），这是一个新颖的框架，通过从无标签测试查询中动态增强训练流，使 LRM 能够自我进化。TTVS 包含两个协同模块：（1）在线变分合成，它将静态测试查询转化为多样化且语义等价的变体动态流，迫使模型学习底层的逻辑问题而非表层模式；（2）测试时混合探索，它在合成变体之间平衡了准确性驱动的利用与一致性驱动的探索。大量实验表明，TTVS 在八种模型架构上均表现出优异的性能。值得注意的是，仅使用无标签的测试时数据，TTVS 不仅超越了其他测试时自适应方法，还优于在大量高质量有标签数据上训练的最先进的有监督强化学习技术。

## Abstract
Despite significant advances in Large Reasoning Models (LRMs) driven by reinforcement learning with verifiable rewards (RLVR), this paradigm is fundamentally limited in specialized or novel domains where such supervision is prohibitively expensive or unavailable, posing a key challenge for test-time adaptation. While existing test-time methods offer a potential solution, they are constrained by learning from static query sets, risking overfitting to textual patterns. To address this gap, we introduce Test-Time Variational Synthesis (TTVS), a novel framework that enables LRMs to self-evolve by dynamically augmenting the training stream from unlabeled test queries. TTVS comprises two synergistic modules: (1) Online Variational Synthesis, which transforms static test queries into a dynamic stream of diverse, semantically-equivalent variations, enforcing the model to learn underlying problem logic rather than superficial patterns; (2) Test-time Hybrid Exploration, which balances accuracy-driven exploitation with consistency-driven exploration across synthetic variants. Extensive experiments show TTVS yields superior performance across eight model architectures. Notably, using only unlabeled test-time data, TTVS not only surpasses other test-time adaptation methods but also outperforms state-of-the-art supervised RL-based techniques trained on vast, high-quality labeled data.

---

## 论文详细总结（自动生成）

以下是对论文《TTVS: Boosting Self-Exploring Reinforcement Learning via Test-time Variational Synthesis》的结构化深入分析总结：

### 1. 核心问题与研究动机
*   **核心问题**：大型推理模型（LRMs）在专业领域（如医疗诊断、航空航天）面临**标注数据匮乏**和**缺乏可验证奖励信号**的挑战。
*   **研究动机**：
    *   现有的强化学习（RL）范式高度依赖大规模高质量标注数据。
    *   现有的测试时训练（TTT）方法通常在**静态查询集**上运行，模型容易过拟合特定的文本模式，而非掌握底层的逻辑推理。
    *   **目标**：探索模型是否能在测试阶段，仅利用无标签的查询（Queries）实现自我进化和逻辑泛化。

### 2. 方法论：TTVS 框架
TTVS（测试时变分合成）由两个协同阶段组成：

*   **阶段一：在线变分合成 (Online Variational Synthesis)**
    1.  **伪标签生成**：对原始测试查询进行多次采样（Rollouts），通过“多数投票”（Majority Voting）确定共识答案作为伪标签。
    2.  **变分数据增强**：利用模型自身将原始查询改写为 $k$ 个语义等价但表述不同的新查询。
    3.  **在线过滤**：设置难度阈值 $[\tau_{low}, \tau_{high}]$ 和长度限制，剔除太简单、太难或格式错误的合成数据，确保训练流的质量。

*   **阶段二：测试时混合探索 (Test-time Hybrid Exploration)**
    1.  **组内探索 (IGE)**：针对单个查询（原始或合成）进行独立的策略更新，旨在最大化该特定问题的准确性（准确性驱动的利用）。
    2.  **跨组探索 (CGE)**：在语义等价的查询簇中混合采样，执行统一的多数投票生成联合伪标签。这迫使模型在不同表述下保持逻辑一致性（一致性驱动的探索）。
    3.  **优化算法**：框架对 RL 算法不可知，实验中主要采用 GRPO（群体相对策略优化），也可兼容 OPO 和 DAPO。

### 3. 实验设计
*   **数据集**：涵盖数学与科学推理，包括 MATH-500、AIME-2024、AMC-2023 和 GPQA（研究生水平科学问题）。
*   **Benchmark 模型**：
    *   **Qwen3 系列** (1.7B, 4B, 8B)
    *   **Qwen2.5 系列** (1.5B, 3B, 7B)
    *   **LLaMA 系列** (3.2-3B, 3.1-8B)
*   **对比方法**：
    *   **有监督 RL 模型**：DeepSeek-R1-Distill (1.5B/7B)、OpenReasoner-Zero-7B、SimpleRL-Zero-7B（这些模型使用了 8.9K 到 800K 不等的标注数据）。
    *   **测试时 RL 方法**：TTRL（仅在原始无标签数据上进行 RL）。

### 4. 资源与算力
*   **硬件环境**：使用了 **8 台 NVIDIA GeForce H800 80GB GPU**。
*   **训练细节**：
    *   优化器：AdamW，采用余弦学习率调度，峰值学习率 $5 \times 10^{-7}$。
    *   采样参数：训练时每条查询采样 32 个响应（Temperature=0.6）。
    *   训练时长：根据数据集大小设置了 10 到 40 个 Episodes 不等。
    *   推理限制：最大生成长度限制为 3072 tokens。

### 5. 实验数量与充分性
*   **实验规模**：在 **8 种不同架构/规模的模型**上进行了验证，跨越了三个主流模型家族。
*   **充分性分析**：
    *   **消融实验**：分别验证了 CGE 和 IGE 模块的贡献，以及不同 RL 算法的兼容性。
    *   **鲁棒性测试**：分析了超参数（过滤阈值、Warmup 步数、采样数量）的影响。
    *   **泛化性验证**：在 DeepMath-103K（防污染数据集）、MedMCQA（医疗领域）和 OpenBookQA（常识推理）上进行了额外测试。
    *   **客观性**：实验对比了同等算力预算下的 TTRL，证明了性能提升源于机制创新而非单纯堆砌算力。整体实验设计非常详尽且具有说服力。

### 6. 主要结论与发现
*   **性能飞跃**：TTVS 在所有测试模型上均显著优于 TTRL。例如在 Qwen3-4B 上，MATH500 准确率提升了 26.3%。
*   **超越有监督学习**：仅使用无标签测试数据，TTVS 优于在数十万标注数据上训练的 SOTA 模型（如 DeepSeek-R1-Distill-7B）。
*   **逻辑鲁棒性**：随着题目难度增加（MATH-500 L1-L5），TTVS 相比基准方法的领先优势扩大，证明其变分合成机制有效弥补了知识鸿沟。
*   **效率优势**：TTVS 生成的答案更简洁（Token 长度更短），且在相同采样数下，显存占用与基准方法持平。

### 7. 优点与亮点
*   **动态增强**：打破了测试时自适应依赖静态数据集的僵局，通过自我合成创造了动态训练流。
*   **双重约束**：通过 IGE 保证“算得准”，通过 CGE 保证“逻辑稳”，有效缓解了 RL 中的过拟合问题。
*   **即插即用**：不改变模型架构，兼容多种策略优化算法，具有极强的通用性。
*   **冷启动能力**：通过微量的专家数据 Warmup，甚至能让不具备指令遵循能力的 Base 模型实现自我进化。

### 8. 不足与局限
*   **模型规模限制**：受限于算力，实验未在超大规模模型（如 32B 或 70B 以上）上进行验证。
*   **模态局限**：目前仅限于纯文本任务，尚未扩展到多模态（视觉-语言）推理领域。
*   **任务类型限制**：主要针对有客观标准答案（可验证奖励）的任务。对于写作、摘要等主观任务，需要引入更复杂的“模型即裁判”（LM-as-a-Judge）机制，这会增加成本和偏见风险。

（完）
